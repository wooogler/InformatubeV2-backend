# Migration `20200518235410-init`

This migration has been generated by Sangwook Lee at 5/18/2020, 11:54:10 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "InformatubeV2-backend"."_Like" (
"A" integer  NOT NULL ,"B" integer  NOT NULL )

CREATE TABLE "InformatubeV2-backend"."_Dislike" (
"A" integer  NOT NULL ,"B" integer  NOT NULL )

CREATE UNIQUE INDEX "_Like_AB_unique" ON "InformatubeV2-backend"."_Like"("A","B")

CREATE  INDEX "_Like_B_index" ON "InformatubeV2-backend"."_Like"("B")

CREATE UNIQUE INDEX "_Dislike_AB_unique" ON "InformatubeV2-backend"."_Dislike"("A","B")

CREATE  INDEX "_Dislike_B_index" ON "InformatubeV2-backend"."_Dislike"("B")

ALTER TABLE "InformatubeV2-backend"."_Like" ADD FOREIGN KEY ("A")REFERENCES "InformatubeV2-backend"."Comment"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "InformatubeV2-backend"."_Like" ADD FOREIGN KEY ("B")REFERENCES "InformatubeV2-backend"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "InformatubeV2-backend"."_Dislike" ADD FOREIGN KEY ("A")REFERENCES "InformatubeV2-backend"."Comment"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "InformatubeV2-backend"."_Dislike" ADD FOREIGN KEY ("B")REFERENCES "InformatubeV2-backend"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200518235410-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,29 @@
+datasource postgresql {
+  provider = "postgresql"
+  url      = env("DATABASE_URL")
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id       Int       @default(autoincrement()) @id
+  name     String    @unique
+  comments Comment[]
+  likeComments Comment[] @relation("Like", references: [id])
+  dislikeComments Comment[] @relation("Dislike", references: [id])
+}
+
+model Comment {
+  id        Int      @default(autoincrement()) @id
+  createdAt DateTime @default(now())
+  text      String
+  time      String
+  url       String
+  likeUsers User[] @relation("Like", references: [id])
+  dislikeUsers User[] @relation("Dislike", references: [id])
+  author    User     @relation(fields: [authorId], references: [id])
+  authorId  Int
+  imageUrl  String
+}
```


